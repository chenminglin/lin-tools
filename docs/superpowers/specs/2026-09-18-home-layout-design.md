# 首页布局精简设计

## 目标

精简首页顶部信息，并提高工具入口在桌面端的展示密度。

## 页面行为

- 首页不显示品牌副标题。
- 首页不显示顶部工具导航栏。
- “本地处理”和格式数量徽章继续显示。
- 进入任一具体工具页后，副标题和顶部工具导航栏保持现状，确保用户仍可快速切换工具。

## 工具入口布局

- 桌面宽度下每行展示 3 个入口。
- 中等宽度下每行展示 2 个入口，避免卡片内容过度拥挤。
- 手机宽度下每行展示 1 个入口。
- 工具顺序、文案、点击行为和视觉状态保持不变。

## 实现范围

- 在 React 页面组件中根据当前页面控制副标题和导航栏是否渲染。
- 调整首页工具网格的 CSS 断点，不修改后端接口或其他工具页面。

## 验证

- 构建前端，确认 TypeScript 与 Vite 构建成功。
- 在首页确认副标题和导航栏不可见，工具页确认两者仍可见。
- 检查桌面、平板和手机宽度下分别为 3、2、1 列。
